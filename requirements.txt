# =============================================================================
# REQUIREMENTS FOR MODAL + FASTHTML + SQLITE TEMPLATE APPLICATION
# =============================================================================
#
# This file lists all Python dependencies needed for the application.
# Modal will use this to build the container image.
#
# Key Components:
# - modal: Serverless cloud platform for running Python
# - python-fasthtml: Modern Python web framework using HTMX
# - Additional dependencies are auto-installed by the frameworks
# =============================================================================

# Modal SDK - for serverless cloud deployment
# https://modal.com/docs
modal>=0.68.0

# FastHTML - Python web framework with HTMX integration
# https://www.fastht.ml/
python-fasthtml>=0.12.0

# Python-dotenv - for local development with .env files
# (Modal Secrets are used in production)
python-dotenv>=1.0.0
